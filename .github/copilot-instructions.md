# Copilot Instructions

## Project Guidelines
- User prefers API tests to run separately from the app: start the app manually, then run Refit tests from a separate Visual Studio window. Do not host the app inside tests.