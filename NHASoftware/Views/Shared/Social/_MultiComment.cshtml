@model NHA.Website.Software.Views.Shared.Social.ViewModels.MultiPostVM

@{
    foreach (var postDTO in @Model.Posts)
    {
        postDTO.UUID = Model.UUID;
        await Html.RenderPartialAsync("Social/_PostComment", postDTO);
    }
}