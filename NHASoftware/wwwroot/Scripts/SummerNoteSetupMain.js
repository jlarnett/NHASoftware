$(document).ready(function () {
    $('#MainPostTextbox').summernote({
        height: 75,
        width: 1000,
        toolbar: [
        // [groupName, [list of button]]
        ],
        disableResizeEditor: true
    });
});